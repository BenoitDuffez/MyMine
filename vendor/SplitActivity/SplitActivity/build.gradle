apply plugin: 'android-library'

dependencies {
    compile SupportLib
    compile AppCompatLib
}

android {
    compileSdkVersion CompileSdkVersion
    buildToolsVersion BuildToolsVersion

    defaultConfig {
        minSdkVersion MinSdkVersion
        targetSdkVersion TargetSdkVersion
    }
}
